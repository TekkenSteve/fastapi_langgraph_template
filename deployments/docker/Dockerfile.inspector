# Official MCP Inspector (interactive MCP server debugging UI) for the dev stack.
FROM node:22-alpine
RUN npm install -g @modelcontextprotocol/inspector
EXPOSE 6274 6277
ENTRYPOINT ["npx", "@modelcontextprotocol/inspector"]
