"""ML domain package — model serving as another domain surface.

Same shape as src/shop/: a backend port (swappable model implementations),
a REST surface (api.py), and — the part agent templates usually miss —
the model is ALSO exposed as a tool the agents can call.
"""
